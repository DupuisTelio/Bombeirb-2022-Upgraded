#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  int next_level;
  char cell;
  int next_map[100];
  FILE *fic = fopen("map_1.txt","r");
  fopen("map_1.txt","r");
  int i=1;
  int j=0;
  cell = fgetc(fic);
  while((cell=fgetc(fic))!=EOF))
  {
      i++;
      if(i%2==0)
      {
          next_map[j]=cell;
          j++;
      }
      cell=fgetc(fic);
   }
   return 0;
}
